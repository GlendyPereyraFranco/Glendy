#include "stdafx.h"
#include "stdio.h"
#include "conio.h"

void main(void)
{
 int i, primer, ultimo, numero; 
 
 printf(" Introduzca ultimo numero: "); 
 scanf("%d", &ultimo); 
 printf(" Los numeros primos son…: "); 
 for(n=1; numero<ultimo; n++) 
 { 
 primer = 1; 
 for(i=2; i<numero; i++) 
 if(n%i == 0) 
 { 
 primer = 0; 
 break; 
 } 
 if(primer) 
 printf("%d\n",numero); 
 } 
 getch(); 
}
